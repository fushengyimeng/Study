1. ps -aux | grep python => 查看进程名中包含python的


2. 修改配置文件
    - vi 要修改的文件名 进入
    - 按i进入修改模式
    - 修改后 按esc 之后:wq保存退出 （:q!不保存退出）
    - source ~/.bashrc 用来使修改的命令生效

    kali linux 可以用nano 要修改的文件名
    修改后 Ctrl+ X   Ctrl + Y 之后enter退出

