1. 先抓包对比 确定加密参数是哪个
2. 定位加密参数的生成位置
3. 查找生成位置的2中方法 1.直接通过该加密参数的名字搜索在哪个js文件里 之后在该文件中搜索该名字 找到疑似位置 打断点 看是否通过这里 如通过代表就是这里
                        2. 通过url中的特殊字符 在xhr/fetch breakpoints地方打断点 之后在call back调用栈中从后往前寻找该加密参数的生成位置
4. 确定找到的加密位置生成的参数 是否跟接口请求的一致 如一致 则定位的位置正确
5. python还原其生成该加密参数的逻辑